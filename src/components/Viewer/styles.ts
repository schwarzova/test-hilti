import { css } from '../../../styled-system/css';

export const tagStyles = css({
  position: 'absolute',
  width: '10px',
  height: '10px',
  backgroundColor: 'red',
  borderRadius: '50%',
  transform: 'translate(-50%, -50%)',
});
