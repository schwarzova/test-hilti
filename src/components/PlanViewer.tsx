import { css } from '../../styled-system/css';

const planStyles = css({
  fontSize: '16px',
});

function TopMenu() {
  return <div className={planStyles}>plan viewer...</div>;
}

export default TopMenu;
